model_name: Foundation-Sec-8B-Reasoning

base_image:
  image: vllm/vllm-openai:v0.17.1

docker_server:
  start_command: bash /app/model/start.sh
  readiness_endpoint: /health
  liveness_endpoint: /health
  predict_endpoint: /v1/completions
  server_port: 8000

resources:
  accelerator: H100
  use_gpu: true

secrets:
  hf_access_token: null

